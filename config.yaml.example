# Alternative to .env for non-secret configuration.
# Copy to config.yaml if needed (optional).

outputDir: output
audioDir: audio
sttProvider: assemblyai
assemblyAiApiKeys: []         # Optional: use multiple AssemblyAI keys
assemblyAiKeyFailureThreshold: 2
assemblyAiKeyCooldownMs: 60000
deepgramApiKeys: []           # Optional: use multiple Deepgram keys
deepgramKeyFailureThreshold: 2
deepgramKeyCooldownMs: 60000
deepgramModel: nova-3          # only used when sttProvider=deepgram
deepgramDiarization: true      # only used when sttProvider=deepgram
openaiWhisperModel: whisper-1   # only used when sttProvider=openai_whisper
maxAudioMB:
splitOverlapSeconds: 2
filenameStyle: title_id   # id | id_title | title_id
audioFormat: mp3
languageDetection: auto   # auto | manual
languageCode: en_us
concurrency: 2
downloadRetries: 2
transcriptionRetries: 2
catalogMaxAgeHours: 168          # TTL for output/_catalog/<channelId>.json; <= 0 disables expiry
pollIntervalMs: 5000
maxPollMinutes: 60
providerTimeoutMs: 120000
csvEnabled: false
assemblyAiCreditsCheck: warn   # warn | abort | none
assemblyAiMinBalanceMinutes: 60
commentsEnabled: false
commentsMax:
ytDlpPath:
